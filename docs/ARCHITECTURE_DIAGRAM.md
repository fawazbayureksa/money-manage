# Clean Architecture Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         HTTP REQUEST                            │
│              GET /api/users?page=1&search=john                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CONTROLLER LAYER                             │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  UserController.GetUsers(c *gin.Context)                 │  │
│  │  - Parse query parameters                                │  │
│  │  - Bind to UserFilterRequest DTO                         │  │
│  │  - Validate input                                        │  │
│  │  - Call service                                          │  │
│  │  - Return HTTP response                                  │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │ UserFilterRequest
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  UserService.GetAllUsers(filter)                         │  │
│  │  - Set default values                                    │  │
│  │  - Apply business logic                                  │  │
│  │  - Call repository                                       │  │
│  │  - Convert Models to DTOs                                │  │
│  │  - Build pagination response                             │  │
│  │  - Hide sensitive data (passwords)                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │ UserFilterRequest
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   REPOSITORY LAYER                              │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  UserRepository.FindAll(filter)                          │  │
│  │  - Build database query                                  │  │
│  │  - Apply filters (WHERE clauses)                         │  │
│  │  - Apply sorting (ORDER BY)                              │  │
│  │  - Apply pagination (LIMIT, OFFSET)                      │  │
│  │  - Count total records                                   │  │
│  │  - Execute query                                         │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │ GORM Query
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                      DATABASE (MySQL)                           │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │  SELECT * FROM users                                     │  │
│  │  WHERE name LIKE '%john%'                                │  │
│  │  ORDER BY id DESC                                        │  │
│  │  LIMIT 10 OFFSET 0                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │ []User, total
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   RESPONSE FLOW (BACK UP)                       │
└─────────────────────────────────────────────────────────────────┘
                             │
                             ▼
                Repository returns: []models.User, int64
                             │
                             ▼
                Service converts: []dto.UserResponse
                Service builds: dto.PaginationResponse
                             │
                             ▼
                Controller returns: HTTP 200 + JSON
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                         HTTP RESPONSE                           │
│  {                                                              │
│    "success": true,                                             │
│    "message": "Users retrieved successfully",                   │
│    "data": {                                                    │
│      "data": [                                                  │
│        {                                                        │
│          "id": 1,                                               │
│          "name": "John Doe",                                    │
│          "email": "john@example.com",                           │
│          "address": "123 Main St",                              │
│          "is_verified": false,                                  │
│          "is_admin": false                                      │
│          // ✅ NO PASSWORD!                                     │
│        }                                                        │
│      ],                                                         │
│      "page": 1,                                                 │
│      "page_size": 10,                                           │
│      "total_items": 25,                                         │
│      "total_pages": 3                                           │
│    }                                                            │
│  }                                                              │
└─────────────────────────────────────────────────────────────────┘
```

## Layer Responsibilities

### 🎮 Controller Layer
```
Responsibility: HTTP handling only
├── Parse request parameters
├── Bind to DTOs
├── Validate input format
├── Call appropriate service method
└── Return HTTP response with status code
```

### 🧠 Service Layer
```
Responsibility: Business logic
├── Validate business rules
├── Call one or more repositories
├── Process/transform data
├── Convert models to DTOs
├── Apply business calculations
└── Handle complex operations
```

### 💾 Repository Layer
```
Responsibility: Database operations
├── Build database queries
├── Apply filters/sorting/pagination
├── Execute CRUD operations
├── Handle database errors
└── Return raw models
```

### 📦 DTO Layer
```
Responsibility: Data contracts
├── Request validation structures
├── Response structures
├── Hide sensitive fields
└── API versioning support
```

## Dependency Flow

```
┌──────────────┐
│    routes    │  (Dependency Injection)
└──────┬───────┘
       │ creates
       ▼
┌──────────────┐     depends on     ┌──────────────┐
│ controller   │ ───────────────────▶│   service    │
└──────────────┘    (interface)     └──────┬───────┘
                                            │ depends on
                                            ▼
                                     ┌──────────────┐
                                     │  repository  │
                                     └──────┬───────┘
                                            │ uses
                                            ▼
                                     ┌──────────────┐
                                     │   database   │
                                     └──────────────┘
```

## Benefits Visualization

```
┌────────────────────────────────────────────────────────────────┐
│                    SEPARATION OF CONCERNS                      │
├────────────────────────────────────────────────────────────────┤
│  Controller  │  HTTP, routing, request/response handling       │
│  Service     │  Business logic, validation, transformations    │
│  Repository  │  Database queries, CRUD operations              │
│  DTO         │  Data validation, API contracts                 │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                         TESTABILITY                            │
├────────────────────────────────────────────────────────────────┤
│  Controller  │  Mock service → Test HTTP handling              │
│  Service     │  Mock repository → Test business logic          │
│  Repository  │  Mock database → Test queries                   │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                        FLEXIBILITY                             │
├────────────────────────────────────────────────────────────────┤
│  Change DB?      │  Only update repository layer               │
│  Change API?     │  Only update controller/DTO layer           │
│  Change logic?   │  Only update service layer                  │
└────────────────────────────────────────────────────────────────┘
```

## Comparison: Laravel vs This Go Architecture

```
┌─────────────────────┬──────────────────────┐
│      Laravel        │      Go (This)       │
├─────────────────────┼──────────────────────┤
│ Route               │ Route                │
│   ↓                 │   ↓                  │
│ Controller          │ Controller           │
│   ↓                 │   ↓                  │
│ Service (optional)  │ Service (required)   │
│   ↓                 │   ↓                  │
│ Repository          │ Repository           │
│   ↓                 │   ↓                  │
│ Eloquent Model      │ GORM Model           │
│   ↓                 │   ↓                  │
│ Database            │ Database             │
│                     │                      │
│ Request Classes     │ DTOs                 │
│ Resource Classes    │ DTOs                 │
│ FormRequest         │ DTO with bindings    │
└─────────────────────┴──────────────────────┘
```

## File Structure Tree

```
my-api/
│
├── dto/                          # 📋 Data Transfer Objects
│   ├── pagination.go            # Reusable pagination logic
│   ├── user_dto.go              # User requests/responses
│   └── bank_dto.go              # Bank requests/responses
│
├── repositories/                # 💾 Database Layer
│   ├── user_repository.go       # User CRUD + queries
│   └── bank_repository.go       # Bank CRUD + queries
│
├── services/                    # 🧠 Business Logic Layer
│   ├── user_service.go          # User business rules
│   └── bank_service.go          # Bank business rules
│
├── controllers/                 # 🎮 HTTP Layer
│   ├── user_controller.go       # User HTTP handlers
│   ├── bank_controller.go       # Bank HTTP handlers
│   └── auth_controller.go       # Auth HTTP handlers
│
├── routes/                      # 🛣️ Route Definitions
│   └── routes.go                # DI + route registration
│
├── models/                      # 📊 Database Models
│   ├── user.go
│   ├── bank.go
│   ├── category.go
│   └── transaction.go
│
├── middleware/                  # 🛡️ HTTP Middleware
│   └── auth_middleware.go
│
├── config/                      # ⚙️ Configuration
│   └── database.go
│
└── utils/                       # 🔧 Utilities
    ├── response.go
    ├── hash.go
    └── genere_token.go
```

This architecture is:
✅ Clean and maintainable
✅ Easy to test
✅ Similar to Laravel (familiar)
✅ Production-ready
✅ Interview-ready
